@{
    ViewData["Title"] = "Sort Books";
}

<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" />
<script src="https://cdnjs.cloudflare.com/ajax/libs/Sortable/1.10.2/Sortable.min.js"></script>

<div class="text-center text-light">
    <h2 class="display-4">Book Replacing</h2>
</div>

@model List<string>

<div class="wrapper">
    <br /> <h3>Arrange the following call numbers</h3><br />
    @foreach (var item in Model)
    {
        <div class="item"><span>@item.ToString()</span><i class="fas fa-bars"></i></div>}
</div>

<br />


<button type="button" name="btnLink" id="btnLink" class="btn-success">Check</button>


<div class="checkwrap">
    @foreach (var item in Model)
    {<div><span>@item.ToString()</span><i class="fas fa-bars"></i></div>}
</div>




<script>
    const dragArea = document.querySelector(".wrapper");
    new Sortable(dragArea, {
        animation: 350
    });

    //code attribution
    //this code for document.query functions was adapted from the following source:
    //Author: Mozilla MDN Web docs
    //url: https://developer.mozilla.org/en-US/docs/Web/API/Document/querySelector
    //end of code attribution

    function checker() {
        var contentsone = document.querySelectorAll("li.unordered");
        var arrayone = Array.from(contentsone);
        var contentstwo = document.querySelectorAll("li.ordered");
        var arraytwo = Array.from(contentstwo);
        var sortednumbers = arraytwo.sort((a, b) => a - b);
        window.showModalDialog('/Home/OpenModelPopup', "WindowPopup", 'width=400px,height=400px');
    }
    //end of attributed code

</script>



@{

}

<style>
    .wrapper .item i {
        font-size: 18px;
        cursor: pointer;
    }

    .wrapper {
        color: white;
    }

    .checkwrap {
        visibility: hidden;
    }

    .wrapper .item {
        color: #fff;
        display: flex;
        margin-bottom: 8px;
        padding: 12px 17px;
        background: #06085f;
        border-radius: 3px;
        align-items: center;
        justify-content: space-between;
        width: 30rem;
    }
</style>




<script src="https://code.jquery.com/jquery-1.10.0.min.js"
        integrity="sha256-2+LznWeWgL7AJ1ciaIG5rFP7GKemzzl+K75tRyTByOE="
        crossorigin="anonymous"></script>
<script>
    $("#btnLink").click(function () {
        $('#ModalPopUp').modal('show');
    })
</script>




<div class="modal fade" id="ModalPopUp" role="dialog">
    <div class="modal-dialog err-pop" style="">
        <div class="modal-content">
            <div class="modal-header">
                <button id="DivClose" type="button" class="close" data-dismiss="modal">&times;</button>
            </div>
            <div class="modal-body" style="text-align:center;">
                <h2>Congrats, youve made it through the first level!, click continue to proceed to the next level!</h2>
                <input class="btn-success" id="Button1" type="button" value="Continue" onclick="window.location.reload();"/>
                
            </div>
        </div>
    </div>
    </div>